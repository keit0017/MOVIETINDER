<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.Salsa">

    <dist:module dist:instant="true" />

    <uses-permission android:name="anroid.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Aktiviteter.ReviewskaermAktivitet"></activity>
        <activity android:name=".Aktiviteter.ChatAkt" />
        <activity android:name=".Aktiviteter.AddFilmActivity" />
        <activity android:name=".Aktiviteter.RegisterAkt" />
        <activity android:name=".Aktiviteter.LoginAkt">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Aktiviteter.MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>